server:
  port: 8080

spring:
  application:
    name: api-gateway

#  cloud:
#    gateway:
#      discovery:
#        locator:
#          enabled: true                # Enable dynamic routes from Eureka
#          lower-case-service-id: true  # Convert service IDs to lowercase
#      routes:
#        - id: user-service
#          uri: lb://user-service
#          predicates:
#            - Path=/users/**

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true

management:
  endpoints:
    web:
      exposure:
        include: "*"       # Expose all actuator endpoints for debugging
  endpoint:
    health:
      show-details: always
